// Find out how we can join two or more strings together.
// You can simply do the following
'string one ' + 'string 2'

// Or you can use the String.prototype.concat() instance method
let str = 'Hello'
console.log(str.concat(' concatenated, ', 'and again concatenated!'));
